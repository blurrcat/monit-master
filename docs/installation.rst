============
Installation
============

At the command line::

    $ easy_install monit-master

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv monit-master
    $ pip install monit-master